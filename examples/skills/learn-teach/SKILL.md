---
name: learn-teach
description: 逐步深入解释概念
argument-hint: <concept>
effort: low
---

# 教我

逐步深入解释概念。

## 用法

```
/learn:teach React hooks         # 学习 React hooks
/learn:teach async/await         # 理解异步模式
/learn:teach SOLID principles    # 学习设计原则
/learn:teach --deep SQL joins    # 深入解释
```

## 使用说明

1. 以概念的**一句话定义**开始
2. 解释**为什么重要**（解决的现实问题）
3. 展示**最小示例**（最简单的代码）
4. 逐行分解示例并注释
5. 展示**实际示例**（真实用例）
6. 指出初学者常犯的**常见错误**
7. 建议**下一步学习的概念**

## 回复格式

```markdown
## [概念名]

**一句话**：[清晰、简单的定义]

### 为什么重要

[1-2 句关于此解决的问题]

### 最小示例

\`\`\`[语言]
// 最简单的演示
[代码]
\`\`\`

**逐行解析**：
- 第 1 行：[解释]
- 第 2 行：[解释]
...

### 实际示例

\`\`\`[语言]
// 真实场景
[代码]
\`\`\`

### 常见错误

1. **[错误]**：[为什么错以及应该怎么做]
2. **[错误]**：[为什么错以及应该怎么做]

### 关键要点

- [要点 1]
- [要点 2]
- [要点 3]

### 下一步学习

- [相关概念 1] — [为什么会关联]
- [相关概念 2] — [为什么会关联]

---

**练习挑战**：[巩固概念的小练习]
```

## 深度模式

### 默认
- 一句话定义
- 一个最小示例
- 一个实际示例
- 2-3 个常见错误

### `--deep`
- 扩展解释（含历史/背景）
- 多个逐步复杂的示例
- 边缘用例和陷阱
- 性能考量
- 与替代方案比较
- 面试相关细节

### `--quick`
- 仅定义
- 单个示例
- 无额外内容

## 适应规则

### 面向初学者
- 使用日常生活中的比喻
- 避免行话（或立即解释）
- 代码中更多注释
- 更小的代码示例

### 面向中级
- 假设基础语法知识
- 关注"为什么"而非"是什么"
- 包含权衡
- 展示惯用模式

### 面向高级主题
- 引用官方文档
- 适当讨论内部实现
- 包含性能影响
- 提及相关模式

## 示例会话

```
用户：/learn:teach useEffect

Claude：## useEffect

**一句话**：useEffect 让你在 React 渲染组件后执行副作用（数据获取、订阅、DOM 修改）。

### 为什么重要

React 组件是渲染 UI 的纯函数。但实际应用需要与外部世界交互——API、浏览器事件、定时器。useEffect 是 React 声明式世界和命令式副作用之间的桥梁。

### 最小示例

\`\`\`jsx
import { useEffect, useState } from 'react';

function Timer() {
  const [seconds, setSeconds] = useState(0);

  useEffect(() => {
    const id = setInterval(() => {
      setSeconds(s => s + 1);
    }, 1000);

    return () => clearInterval(id);  // 清理
  }, []);  // 空数组 = 运行一次

  return <div>{seconds}s</div>;
}
\`\`\`

**逐行解析**：
- `useEffect(() => {...}, [])`：首次渲染后运行此函数
- `setInterval(...)`：启动定时器（副作用）
- `return () => clearInterval(id)`：组件卸载时清理
- `[]`：空依赖数组 = 仅在挂载时运行

### 常见错误

1. **缺少依赖数组**：没有 `[]`，effect 每次渲染都运行
2. **忘记清理**：没有 return 函数会导致定时器/订阅泄漏
3. **过期闭包**：使用旧状态值而非函数式更新

---

**练习挑战**：添加一个暂停和恢复定时器的按钮。
```

## 适合 /learn:teach 的主题

| 类别 | 示例 |
|----------|----------|
| **React** | hooks、context、suspense、server components |
| **JavaScript** | 闭包、Promise、事件循环、原型 |
| **TypeScript** | 泛型、映射类型、工具类型 |
| **模式** | SOLID、DI、组合、工厂 |
| **后端** | REST、GraphQL、认证、缓存 |
| **数据库** | 索引、join、事务、规范化 |
| **DevOps** | 容器、CI/CD、基础设施即代码 |

$ARGUMENTS
